---
title: Razor DEX · The pioneer dex on Movement network
content:
  width: 100%
layout: landing
logo: false
---

import { HomePage } from 'vocs/components'
import styles from './index.module.css'
import { actions } from './data'
import { ArrowUpRight as LinkIcon } from 'react-feather'

<HomePage.Root>
  <div className="space-y-8 flex flex-col items-center max-md:items-center">
    <img
      className="h-[72px] max-sm:h-[60px]"
      src="/logo.png"
      alt="razor logo"
    />
  </div>
  <HomePage.Tagline className="text-3xl font-medium">
    Welcome to Razor DEX Docs
  </HomePage.Tagline>
  <div className={styles.row}>
    {actions.map((action) => (
      <HomePage.Button className={styles.shadowCard} href={action.to}>
        <div className={styles.topSection}>
          <div className={styles.iconWrapper}>
            <action.icon style={{ width: '24px' }} />
          </div>
          <div className={styles.linkIconWrapper}>
            <LinkIcon />
          </div>
        </div>
        <div className="text-xl font-medium text-black dark:text-white">{action.title}</div>
        <div className="text-[14px] text-[#919193]">{action.text}</div>
      </HomePage.Button>
    ))}
  </div>
</HomePage.Root>

